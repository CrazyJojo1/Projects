#New things:
  # class() and typeof(), both can be used interchangeably.
#as.integer() will convert a double type into a int type
#as.integer() will fall explicit coercion.


#practice
"a"
"Hello, world"
typeof("a")
class("a")
2
class(2)
2L
class(3L)
as.integer(12.7)
as.integer("a")
?as.integer
#as.integer() function is used to create an integer type
#this is called explicit coercion

class(as.integer(12.7))
  T
F  
class(T)
